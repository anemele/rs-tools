[package]
name = "an2cn"
version = "0.1.0"
edition = "2024"

[dependencies]
chinese-number = {
    version = "0.8.2",
    default-features = false,
    features = ["number-to-chinese"]
}
clap = { version = "4.6.6", features = ["derive"] }
